---
description: 打包 VS Code 扩展为 .vsix 文件
---

# 打包扩展

// turbo-all

1. 进入 extension 目录并执行打包命令：
```shell
cd d:\work\dev\Antigravity-Auto-Accept\extension && npm run package
```

打包完成后会在 `extension/` 目录下生成 `antigravity-auto-accept-{version}.vsix` 文件。
